rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 테스트 모드: 모든 읽기/쓰기 허용 (30일 후 수정 필요)
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 3, 9);
    }
  }
}
